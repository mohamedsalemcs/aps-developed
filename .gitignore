# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment
venv/

# Django
db.sqlite3
db.sqlite3.backup
data_backup.json
db.sqlite3-journal
staticfiles/
media/

# NOTE: CMS-uploaded images (uploads/) and the normalized partner-logo tiles
# (partners_framed/) are intentionally TRACKED now, so they deploy to the test
# server via git (the server's DB references them; without the files the images
# 404). They are regenerated at runtime but must be present on first deploy.
# media-replace originals backup (runtime)
static/assets/images/.originals/
# E2E report output (generated)
ops/e2e/report/

# runserver redirect logs (dev)
server.log
server.out

# Environment / secrets
.env
*.env
ops/secrets.local.ps1

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
